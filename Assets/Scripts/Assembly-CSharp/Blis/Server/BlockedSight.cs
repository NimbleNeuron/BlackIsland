using Blis.Common;

namespace Blis.Server
{
	
	[SkillScript(SkillId.BlockedSight)]
	public class BlockedSight : CrowdControlScript
	{
		
		protected override void Finish(bool cancel = false)
		{
			base.Finish(cancel);
		}
	}
}